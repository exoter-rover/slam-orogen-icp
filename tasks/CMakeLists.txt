# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(icpTaskLib)
ADD_LIBRARY(${ICP_TASKLIB_NAME} SHARED 
    ${ICP_TASKLIB_SOURCES})
add_dependencies(${ICP_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ICP_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ICP_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ICP_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ICP_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ICP_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ICP_TASKLIB_HEADERS}
    DESTINATION include/orocos/icp)

